---
name: plan
description: Plan a feature across Clean Architecture layers before any code is written. Invokes the feature-planner agent to produce a reviewable plan.md artifact.
allowed-tools: Agent
---

Invoke the `feature-planner` agent now.

Pass no arguments — the agent will gather intent interactively starting at Phase 0.
