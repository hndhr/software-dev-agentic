{
  "permissions": {
    "allow": [
      // Common entries to add per project:
      // "Bash(xcodebuild:*)",
      // "Bash(swift:*)",
      // "Bash(git log:*)",
      // "Bash(git diff:*)"
    ],
    "deny": []
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "PROJECT_ROOT/hooks/require-feature-orchestrator.sh"
          }
        ]
      }
    ]
  }
}
