# dependency_injection — flutter-qontak-chat

| Pattern | Description |
|---|---|
| `chat_di` | Qontak Chat uses `get_it` manually — feature packages expose static `register*()` methods called in dependency order. |
