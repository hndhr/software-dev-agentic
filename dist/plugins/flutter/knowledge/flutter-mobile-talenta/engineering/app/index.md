# app — flutter-mobile-talenta

| Pattern | Description |
|---|---|
| `hybrid_embedding` | Talenta runs Flutter as a guest module embedded inside the native Android/iOS Talenta host via `brick_way`. |
| `module_registration` | Flutter features registered as explicit modules via `BaseModule` + `TalentaModuleManager`. |
