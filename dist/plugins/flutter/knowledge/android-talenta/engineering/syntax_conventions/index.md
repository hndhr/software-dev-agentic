# syntax_conventions — android-talenta

| Pattern | Description |
|---|---|
| `null_safety_extensions` | Use `.orEmpty()`, `.orZero()`, `.orFalse()`, `.orTrue()` from `com.mekari.commons.extension` — never `?: ""` or `?: 0` inline. |
