# utilities — flutter-mobile-jurnal

| Pattern | Description |
|---|---|
| `local_storage` | Local storage abstracted through `BaseLocalRepository` and `BaseStorage` in `jurnal_core`. |
| `logger` | Multi-logger setup in `jurnal_core` with specialized loggers per concern — no DI registration needed. |
