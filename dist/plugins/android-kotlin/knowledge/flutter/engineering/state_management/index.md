# state_management — flutter

| Pattern | Description |
|---|---|
| `bloc` | BLoC for event-driven state management — widgets dispatch events, BLoC emits immutable states. |
| `cubit` | Use Cubit when there are no events — simpler than BLoC for state toggles and shared global state. |
