# app — flutter-qontak-crm

| Pattern | Description |
|---|---|
| `module_registration` | CRM uses a Melos monorepo with all features as independent packages implementing `BaseModule` from `crm_core`. |
